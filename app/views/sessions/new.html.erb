<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Login</title>

    <link href="https://fonts.googleapis.com/css?family=Muli" rel="stylesheet">

    <style>
      p {
        font-family: 'Muli', sans-serif;
      }
    </style>

  </head>
  <body>
    <div class="container">
      <div class="card card-login mx-auto mt-5">
        <div class="card-header">Login</div>
          <div class="card-body">
          <%= form_for(:session, url: login_path) do |f| %>
            <p><%= f.label :username, "Username:" %>
            <%= f.text_field :username %></p><br/>
            <p><%= f.label :password, "Password:" %>
            <%= f.password_field :password %></p><br/>
            <%= f.submit "Login", class: 'btn btn-outline-success' %>
          <% end %>
        </li>
      </ul>
      <div class="card-block">
        <%= link_to "Create Account", new_user_path %><br>
        <%= link_to "(Forgot My Password)", password_reset_path %>
      </div>
    </div>
  </div>
</div>

  </body>
</html>
